<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="R4Everyone.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:R4Everyone.Views"
    mc:Ignorable="d"
    DataContext="{x:Bind ViewModel}">

    <NavigationView
        IsBackButtonVisible="Collapsed"
        AlwaysShowHeader="False"
        SelectionChanged="NavView_ItemInvoked"
        PaneTitle="R4Everyone">
        <NavigationView.MenuItems>
            <NavigationViewItem IsSelected="True" 
                                Content="Games" 
                                Tag="GamesView">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE7FC;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem Content="Database" 
                                Tag="DatabaseView">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE946;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem Content="Find Games" 
                                Tag="FindGamesView">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xF6FA;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Frame x:Name="MainFrame" Padding="20" />
    </NavigationView>
</Page>
