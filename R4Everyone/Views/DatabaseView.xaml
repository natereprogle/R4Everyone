<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="R4Everyone.Views.DatabaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:R4Everyone.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:binary4Everyone="using:R4Everyone.Binary4Everyone"
    mc:Ignorable="d"
    DataContext="{x:Bind ViewModel, Mode=OneWay}">

    <StackPanel Padding="10" HorizontalAlignment="Left">
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock VerticalAlignment="Center">Database Title</TextBlock>
            <TextBox VerticalAlignment="Center" Width="200" Text="{x:Bind ViewModel.DatabaseTitle, Mode=TwoWay}"/>
        </StackPanel>
        <CheckBox Content="Enabled" IsChecked="{x:Bind ViewModel.IsEnabled}" />
        <TextBlock Text="{x:Bind ViewModel.GamesCountMessage, Mode=OneWay}" />
    </StackPanel>
</Page>
